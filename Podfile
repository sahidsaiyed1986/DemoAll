# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DemoAll' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DemoAll
    pod 'GoogleMaps'
    pod 'GooglePlaces'

  target 'DemoAllTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DemoAllUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
